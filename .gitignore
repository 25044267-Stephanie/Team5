# Secrets — never commit any env file except the tracked example template.
.env
*.env
!.env.example
Team5_HotelManagement_Project.env
*.pem
*.key
id_rsa
id_ed25519

# EC2 deploy state (records previous/current APP_IMAGE URIs only — still local).
state/
reports/
backups/

# Ansible private inventory (may contain hostnames / key paths).
ansible/hosts.ini
ansible/hosts.yml
ansible/*.retry

__pycache__/
*.pyc
*.pyo
.pytest_cache/
.ruff_cache/
.venv/
.venv-pin/
mysql-data/
*.egg-info/
.coverage
htmlcov/
reports/
trivy-report.json
data.json.bak
